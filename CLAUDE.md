# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際に従うポリシーです。

## プロジェクト概要

食材在庫管理・大人用献立提案アプリ（`mealtracking-app/`）。React + Vite + TypeScript、Tailwind CSS、Dexie.js（IndexedDB）、vite-plugin-pwa構成のクライアントサイド完結アプリ。2026年8月に離乳食トラッキング機能（M1〜M3）からスコープを再定義した（経緯は `docs/mealtracking_usecase_ui.md` 10章参照、旧実装は `archive/mealtracking-m1-m3` ブランチに保全）。開発方針・データモデル・実装計画は `docs/` ディレクトリを参照。GitHub Pages（`.github/workflows/deploy.yml`、`main`へのpushで自動デプロイ）でホスティングしている。

## ブランチ運用ポリシー

- **`main` はリリースブランチです。`main` 上で直接作業（コミット・プッシュ）しないでください。**
- 新しい変更は必ず専用ブランチを切って行います。ブランチ名は変更内容が分かるプレフィックスを付けます。
  - 機能実装：`feat/<概要>`（例：`feat/weekly-menu-table`）
  - バグ修正：`fix/<概要>`（例：`fix/record-time-timezone`）
  - ドキュメントのみの変更：`docs/<概要>`
  - その他（設定変更・リファクタ等）：`chore/<概要>`
- 変更が完了したら Pull Request を作成し、レビュー後に `main` にマージします。
- ローカル/セッション側の制約で別名のブランチ（例：セッション専用ブランチ）上での作業を指示されている場合はそちらに従いますが、その場合も `main` への直接コミットは行いません。

## ドキュメント参照ポリシー

- `docs/` ディレクトリに開発方針・データモデル設計・実装計画（ステップ単位の指示書）を置いています。まずは `docs/README.md` を参照してください。
- 実装作業を始める前に、関連する `docs/` 配下のドキュメントを確認し、既存の設計判断・データモデル・命名規則との一貫性を保ってください。
- 設計上の重要な決定を行った場合や、既存ドキュメントとの食い違いが生じた場合は、コードの変更と合わせて該当ドキュメントも更新してください。
- 計画にない追加要望を実装した場合も、影響するデータモデル・ユースケースがあれば `docs/` の該当ドキュメントに追記してください。

## 開発の基本コマンド

```bash
cd mealtracking-app
npm run dev      # 開発サーバー起動
npm run build    # 型チェック + 本番ビルド
```

## Claude Codeによる実装方針

- 実装前に必ず `docs/README.md` から関連ドキュメントを確認し、既存のデータモデル・命名規則との一貫性を保つこと。特にDexieのテーブル定義・バージョン番号は、設計書のサンプルコードをそのまま適用するのではなく、リポジトリの実際のスキーマ状態を確認した上で整合させること。
- 設計書のサンプルコードと実際のリポジトリの状態（DBバージョン、既存テーブル・関数、インデックスの有無等）に食い違いがある場合は、動作するように調整した上で、その判断と理由を該当設計書に実装メモとして残すこと（コード変更のみで済ませない）。
- 初期データ・シードデータの具体的な品目、既存機能への破壊的変更、DBスキーマの移行方針など、設計書・指示書が「別途検討する」「確認の上決定する」としている判断ポイントは、勝手に決めずユーザーに確認すること。逆に、画面遷移の具体的なUI（タブの配置等）のような、設計書の意図を損なわない範囲の実装細部は、既存のUIパターン（Tailwindのスタイル、`labels.ts`のラベル定義方式等）を踏襲して自律的に判断してよい。
- 実装指示書がステップ単位に分割されている場合は、ステップごとにコミットする。各ステップ完了後は `npm run build` で型チェック・ビルドが通ることを確認し、対応する `docs/` のチェックリストにチェックを入れる。
- 計画にない追加要望を実装した場合も、影響するデータモデル・ユースケースがあれば該当ドキュメントに追記する。
