# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際に従うポリシーです。

## プロジェクト概要

離乳食トラッキングアプリ（`mealtracking-app/`）。React + Vite + TypeScript、Tailwind CSS、Dexie.js（IndexedDB）、vite-plugin-pwa構成のクライアントサイド完結アプリ。開発方針・データモデル・実装計画は `docs/` ディレクトリを参照。GitHub Pages（`.github/workflows/deploy.yml`、`main`へのpushで自動デプロイ）でホスティングしている。

## ブランチ運用ポリシー

- **`main` はリリースブランチです。`main` 上で直接作業（コミット・プッシュ）しないでください。**
- 新しい変更は必ず専用ブランチを切って行います。ブランチ名は変更内容が分かるプレフィックスを付けます。
  - 機能実装：`feat/<概要>`（例：`feat/weekly-menu-table`）
  - バグ修正：`fix/<概要>`（例：`fix/record-time-timezone`）
  - ドキュメントのみの変更：`docs/<概要>`
  - その他（設定変更・リファクタ等）：`chore/<概要>`
- 変更が完了したら Pull Request を作成し、レビュー後に `main` にマージします。
- ローカル/セッション側の制約で別名のブランチ（例：セッション専用ブランチ）上での作業を指示されている場合はそちらに従いますが、その場合も `main` への直接コミットは行いません。

## ドキュメント参照ポリシー

- `docs/` ディレクトリに開発方針・データモデル設計・実装計画（ステップ単位の指示書）を置いています。まずは `docs/README.md` を参照してください。
- 実装作業を始める前に、関連する `docs/` 配下のドキュメントを確認し、既存の設計判断・データモデル・命名規則との一貫性を保ってください。
- 設計上の重要な決定を行った場合や、既存ドキュメントとの食い違いが生じた場合は、コードの変更と合わせて該当ドキュメントも更新してください。
- 計画にない追加要望を実装した場合も、影響するデータモデル・ユースケースがあれば `docs/` の該当ドキュメントに追記してください。

## 開発の基本コマンド

```bash
cd mealtracking-app
npm run dev      # 開発サーバー起動
npm run build    # 型チェック + 本番ビルド
```
